{{- if .Values.pvFollowNode.hostPath }}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.prefix }}{{ .Values.pvFollowNode.name }}
spec:
  {{- if .Values.nodeName }}
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - {{ .Values.nodeName }}
  {{- end }}
  storageClassName: {{ .Values.pvcFollowNode.storageClassName }}
  capacity:
    storage: {{ .Values.pvcFollowNode.resources.requests.storage }}
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: {{ .Values.pvFollowNode.hostPath }}
{{- end }}